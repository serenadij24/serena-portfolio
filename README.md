Local dev server

Run the simple static server and open the site:

```bash
node serve.mjs
# then open http://localhost:3000 in your browser
```

Files created:
- `index.html` — single-page portfolio (inline styles)
- `serve.mjs` — tiny static server to serve project root on port 3000

Notes:
- The resume `Serena_Dijkhuizen.pdf` and the reference screenshot `studiolinear.com_ (1).png` are used from the project root.
- If you want me to start the server and screenshot the page, tell me and I'll run `node serve.mjs` for you and create a screenshot.
# serena-portfolio
